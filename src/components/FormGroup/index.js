import {
  FormControl,
  FormLabel,
  FormErrorMessage,
  useColorModeValue,
} from "@chakra-ui/react";
import Input from "./components/Input";

export default (props) => {
  return (
    <FormControl>
      <Input />
    </FormControl>
  );
};
